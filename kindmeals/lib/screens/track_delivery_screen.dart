// TODO Implement this library.
import 'package:flutter/material.dart';

class TrackDeliveryScreen extends StatelessWidget {
  const TrackDeliveryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Track Delivery'), backgroundColor: Colors.green),
      body: const Center(child: Text('Track your donation delivery.')),
    );
  }
} 